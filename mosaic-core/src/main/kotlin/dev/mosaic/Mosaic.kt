package dev.mosaic

/**
 * Library metadata for `mosaic-core`. Real public API (`EmbeddingTable`,
 * `Initializer`, `VectorOps`, `TesseraEmbeddings`) lands in Phase 1+.
 */
public object Mosaic {
    /** Current library version, kept in sync with `gradle.properties` by the release workflow. */
    public const val VERSION: String = "0.1.0-SNAPSHOT"
}
